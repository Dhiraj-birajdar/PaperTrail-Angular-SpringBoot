spring:
  application:
    name: PaperTrail-SpringBoot-RestAPI

  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 10MB
#      max-request-size: 10MB
  security:
    user: { name: dev, password: dev, roles: ADMIN }

springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1/